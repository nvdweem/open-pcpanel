pluginManagement {
  repositories {
    maven { url 'https://repo.spring.io/snapshot' }
    gradlePluginPortal()
  }
}
rootProject.name = 'open-pcpanel'

include 'core'
include 'plugin'
include 'volumecontrol'
